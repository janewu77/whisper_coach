Small count / notification badge or a bare status dot.

```jsx
<Badge>14</Badge>
<Badge tone="danger">3</Badge>
<Badge dot tone="brand" />
```

Tones: `brand` (green), `neutral`, `danger`. Use `dot` for a minimal presence/status indicator with no number.
