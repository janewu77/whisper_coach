A single player token positioned on the pitch by `x`/`y` percentages.

```jsx
<PlayerDot name="O.S" pos="ST" x={50} y={22} selected />
```

Used inside `<Pitch>`. Selected/hover states fill with brand green. Omit `x`/`y` to render inline (e.g. in a legend).
