# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# Local DB & secrets
*.db
.env
# uv.lock is committed for reproducible Docker builds.

# Pitch deck is sourced from docs/ at build time; the local copy (made by
# scripts/sync-deck.sh for local dev) is not committed.
app/static/pitch-deck.pdf

# Flutter web build output (made by scripts/build-web.sh / the Docker build).
app/static/app/
